{
  "_comment": "Test network config — bind to USB ethernet NIC for isolated DHCP+DNS testing",
  "_note": "USB ethernet: enx5c857e306c10, main NIC: eno1. Run nat-enable.sh for internet access.",
  "domain": "test.lantern",
  "interface": "enx5c857e306c10",

  "dhcp": {
    "enabled": true,
    "interface": "enx5c857e306c10",
    "subnet": "10.99.0.0/24",
    "range_start": "10.99.0.100",
    "range_end": "10.99.0.200",
    "gateway": "10.99.0.1",
    "dns_servers": ["10.99.0.1"],
    "default_ttl": "5m",
    "static_ttl": "24h",
    "lease_file": "/var/lib/lantern/test-leases.json"
  },

  "dns": {
    "listen": "10.99.0.1:53",
    "name_format": {
      "with_hostname": "{hostname}.test.lantern",
      "fallback": "dhcp-{octet:03d}.test.lantern"
    }
  },

  "upstream": {
    "doh_url": "https://1.1.1.1/dns-query",
    "fallback_servers": ["8.8.8.8:53"],
    "cache_max_entries": 10000,
    "cache_db": "/var/lib/lantern/test-cache.db"
  },

  "blocklists": [
    {
      "url": "https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts",
      "enabled": true,
      "update_interval": "24h"
    }
  ],

  "static_hosts": [],

  "fingerprint": {
    "enabled": false
  },

  "web": {
    "enabled": true,
    "listen": "0.0.0.0:8080"
  },

  "events": {
    "per_host_limit": 100
  },

  "logging": {
    "level": "debug",
    "format": "text"
  }
}
